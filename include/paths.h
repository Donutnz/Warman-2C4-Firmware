#pragma once

// Paths

/*
Exact implementation is WIP
I wonder if there's some way to have pyTurtle involved?

Currently only using point turns.
*/

// (rotation, distance) ? Iterate thru?
const extern int testPath[3][2] = {
    {0, 100},
    {90, 50},
    {-90, 100}
};